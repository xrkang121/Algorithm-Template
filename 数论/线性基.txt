vector<int> basic(64, 0);
bool insert_basic(int x){
    for(int i = 63;i >= 0;i--){
        if(((x >> i) & i) == 0){
            continue;
        }

        if(basic[i] == 0){
            basic[i] = x;
            return true;
        }

        x ^= basic[i];
    }

    return false;
}
